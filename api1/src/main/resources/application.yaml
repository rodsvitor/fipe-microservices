spring:
  application:
    name: api1

  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: redis

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

api2:
  base-url: http://localhost:8081

